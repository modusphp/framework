language: php
php:
  - 5.5
  - 5.6
script:
  - composer install
  - phpunit
after_success:
  - curl -OL https://squizlabs.github.io/PHP_CodeSniffer/phpcs.phar
  - php phpcs.phar -n --standard=PSR1,PSR2 src/